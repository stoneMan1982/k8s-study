# 文件系统

## NFS (Network File System)：跨平台共享的“老大哥”  
它是什么：NFS 是一个协议，而非一个文件系统。它允许一台机器（NFS客户端）通过网络，像访问本地硬盘一样访问另一台机器（NFS服务器）上的文件 。

怎么用：你可以在服务器上用/etc/exports文件配置共享目录，然后在客户端用mount命令挂载。它非常成熟，几乎所有Linux/Unix系统都原生支持 。

适用场景：

开发/测试环境：在团队间共享代码或编译输出 。

容器持久化存储：作为Kubernetes中的PersistentVolume，为容器提供共享存储 。

媒体编辑与渲染：多个工作站共享视频素材和渲染输出文件 。

## ZFS：数据完整性的“守护神”  
它是什么：ZFS 是一个集文件系统和卷管理器于一身的先进文件系统，最初由Sun公司为Solaris系统开发 。它最大的特点是对数据完整性的极致追求。

核心亮点：

保证数据不损坏：对写入的每一个数据块都计算校验和。读取时自动验证，发现硬件返回了错误数据，它能利用自身的RAID-Z或镜像功能自动修复，确保上层应用拿到的数据永远是正确的 。

功能丰富：支持极速快照和克隆、实时透明压缩、数据去重等 。

高性能缓存：通过将内存作为自适应替换缓存（ARC），极大地提升读性能 。

适用场景：

企业关键数据库：需要最高级别数据保护，如Oracle、PostgreSQL数据库后端存储 。

虚拟化存储：作为VMware或Proxmox VE的后端存储，利用快照和克隆功能快速部署虚拟机 。

企业级NAS/文件服务器：为海量数据提供安全、高效的存储池 。

## Ceph：云原生的“弹性架构师”  
它是什么：Ceph 是一个开源的、统一的分布式存储系统。它的设计目标是“没有单点故障”，能在一个集群上同时提供对象、块和文件三种存储服务 。

核心亮点：

完全分布式：所有组件都可水平扩展，没有中心节点，性能和容量随节点增加而线性增长 。

聪明的算法：用CRUSH算法代替了传统的元数据查询表，知道数据具体存在哪，无需查表，效率极高 。

自我修复与管理：节点故障时，数据会自动在集群内进行再平衡和恢复 。

适用场景：

OpenStack或Kubernetes后端：为云平台提供统一、可扩展的块存储（RBD）和对象存储（RGW） 。

对象存储：提供兼容Amazon S3的接口，用于海量数据的归档、备份，如存储图片、视频、日志文件 。

大规模高性能计算：提供并行的CephFS作为共享文件系统 。